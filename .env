# .env.development (save this in your frontend root directory)
VITE_API_URL=http://localhost:5000/api

# .env.production (save this in your frontend root directory)
VITE_API_URL=/api  # This assumes you'll serve the frontend and backend from the same domain in production